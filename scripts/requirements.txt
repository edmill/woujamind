# Requirements for variable frame detection script
# Install with: pip install -r scripts/requirements.txt

pillow>=10.0.0
numpy>=1.24.0
scipy>=1.10.0  # Optional but recommended for better performance (script works without it)

